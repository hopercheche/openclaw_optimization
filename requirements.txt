# EvalScope fork revision with task-level token metrics for external agents.
evalscope @ git+https://github.com/Asuna-L/evalscope.git@openclaw

# AgentScope-based AliyunCS CLI and the optional FastAPI/Redis service in main.py.
agentscope[service,storage]==2.0.3
